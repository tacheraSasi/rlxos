name: Build and Release
on:
  workflow_dispatch:
  push:

env:
  CACHE_PATH: ${{secrets.CACHE_PATH}}
  VERSION: ${{ startsWith(github.ref, 'refs/tags/') && github.ref_name || github.run_number }}
  CHANNEL: ${{ startsWith(github.ref, 'refs/tags/') && 'stable' || 'dev' }}
  SIGN_KEYS_PATH: ${{secrets.SIGN_KEYS_PATH}}
  SOURCEFORGE_PREFIX: ${{secrets.SOURCEFORGE_PREFIX}}

jobs:
  build-world:
    name: Build world
    runs-on: self-hosted
    timeout-minutes: 47000

    steps:
      - uses: actions/checkout@v1
        with:
          token: ${{ secrets.CI_TOKEN }}
          submodules: true
          fetch-depth: 0
      - name: Add sign keys
        run: rm -rf files/sign-keys && cp -r ${SIGN_KEYS_PATH} files/sign-keys

      - name: Write changelog
        run: |-
          set -e
          git fetch --force --tags --prune

          if [ "${GITHUB_REF_TYPE}" = "tag" ]; then
            current_ref="${GITHUB_REF_NAME}"
            current_commit="$(git rev-list -n 1 "${current_ref}")"
            previous_tag="$(git describe --tags --abbrev=0 "${current_commit}^" 2>/dev/null || true)"
          else
            current_ref="HEAD"
            previous_tag="$(git describe --tags --abbrev=0 2>/dev/null || true)"
          fi

          if [ -n "${previous_tag}" ]; then
            git log --format='- %s' "${previous_tag}..${current_ref}" > ChangeLog
          else
            git log --format='- %s' "${current_ref}" > ChangeLog
          fi

      - name: cleanup
        run: |
          rm -rf "${CACHE_PATH}/cache/system-*.pkg" "${CACHE_PATH}/cache/system-*.pkg.dbg"
          rm -rf "${CACHE_PATH}/cache/installer-*.pkg" "${CACHE_PATH}/cache/installer-*.pkg.dbg"

      - name: write local.conf
        run: |
          cat <<EOF > local.conf.yml
          arch: x86_64
          cache-path: ${CACHE_PATH}
          workspace-path: workspaces

          version: ${VERSION}
          variables:
            channel: ${CHANNEL}

          workspace-push: false
          workspace-message: avyos workspace update
          EOF
      - name: Build System image
        run: go run avyos.dev/tools/ignite build installer/image.yml

      - name: Build sysext extensions
        run: |-
          for ext in extensions/sdk.yml extensions/qemu.yml extensions/bspwm.yml extensions/nvidia.yml ; do
            go run avyos.dev/tools/ignite build $ext
          done
        #  elements/extensions/i3.yml elements/extensions/openjdk.yml elements/extensions/sway.yml elements/extensions/wine.yml elements/extensions/wm-kit-x11.yml elements/extensions/wm-kit.yml

  update-system-image:
    name: Update System image
    runs-on: self-hosted
    needs: [build-world]
    steps:
      - uses: actions/checkout@v1
        with:
          token: ${{ secrets.CI_TOKEN }}
          submodules: true

      - name: Cleanup old images
        if: "!startsWith(github.ref, 'refs/tags/')"
        run: |
            mkdir -p ${CACHE_PATH}/releases/${CHANNEL}/
            cd ${CACHE_PATH}/releases/${CHANNEL}/
            find . -maxdepth 1 -name "avyos-*-${CHANNEL}.img" -printf '%T@ %f\n' \
              | sort -rn \
              | awk 'NR > 10 {print $2}' \
              | while read -r image; do
                  rm -f -- "${image}" "${image}.xz" "${image}.zsync"
                done

      - name: Checkout system image
        run: |
              mkdir -p ${CACHE_PATH}/releases/${CHANNEL}/
              rm -rf release-checkout/ && mkdir -p release-checkout
              make  checkout ELEMENT=installer/image.yml DESTDIR=${PWD}/release-checkout
              cp release-checkout/avyos-${CHANNEL}-installer.iso ${CACHE_PATH}/releases/${CHANNEL}/avyos-${VERSION}-${CHANNEL}-installer.iso
              (cd ${CACHE_PATH}/releases/${CHANNEL}/;
                zsyncmake -b 2048 -C -u http://repo.avyos.dev/releases/${CHANNEL}/avyos-${VERSION}-${CHANNEL}-installer.iso avyos-${VERSION}-${CHANNEL}-installer.iso
              )

  publish-system-image:
    name: Publish system image
    runs-on: self-hosted
    environment: deploy
    needs: [update-system-image]
    steps:
      - uses: actions/checkout@v1
        with:
          token: ${{ secrets.CI_TOKEN }}
          submodules: true

      - name: Upload to sourceforge
        run: |-
            scp -pr ${CACHE_PATH}/releases/${CHANNEL}/avyos-${VERSION}-${CHANNEL}-installer.iso ${SOURCEFORGE_PREFIX}/${CHANNEL}/

  publish-updates:
    name: Publish Updates
    runs-on: self-hosted
    needs: [update-system-image]
    steps:
      - uses: actions/checkout@v1
        with:
          token: ${{ secrets.CI_TOKEN }}
          submodules: true

      - name: Add sign keys
        run: rm -rf files/sign-keys && cp -r ${SIGN_KEYS_PATH} files/sign-keys

      - name: Publish systemd-sysupdate artifacts
        run: make  updates

      - name: Publish extensions
        run: |
          for ext in extensions/sdk.yml extensions/qemu.yml extensions/bspwm.yml extensions/nvidia.yml ; do
            make  ELEMENT=$ext update-sysext
          done
