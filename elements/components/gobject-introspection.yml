id: gobject-introspection
version: 1.86.0
about: Describe the program APIs and collect them in a uniform, machine readable format
sources:
  - https://download.gnome.org/sources/gobject-introspection/%{version:2}/gobject-introspection-%{version}.tar.xz
build-dir: gobject-introspection-%{version}

build-depends:
  - components/glib-no-gir.yml

depends:
  - components/glibc.yml